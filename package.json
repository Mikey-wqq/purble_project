{
  "scripts": {
    "clean": "rimraf dist && rimraf build",
    "bundle": "webpack",
    "build": "npm run clean && npm run bundle"
  },
  "devDependencies": {
    "rimraf": "^6.0.1",
    "webpack": "^5.98.0",
    "webpack-cli": "^6.0.1"
  }
}
